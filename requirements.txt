# Direct dependencies only; transitive packages are resolved by pip.
# Versions are those the reported results were produced with.

# Model / training
torch==2.9.1
torchvision==0.24.1

# Numerics and metrics
numpy==2.3.4
scipy==1.16.3
scikit-learn==1.8.0
pandas==3.0.0

# Seismic I/O (SEG-Y)
segyio==1.9.14

# Plotting
matplotlib==3.10.7

# Experiment tracking (optional; used by main.py and the algorithms/ framework)
wandb==0.25.0

# MedMNIST 2D loaders (BloodMNIST / OrganCMNIST / OrganSMNIST)
medmnist==3.0.2
