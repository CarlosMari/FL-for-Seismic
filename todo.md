# Todo

On label shift, the local-accuracy gap is the prior: true log π on the global model closes 87–101% of it, and the prior never has to leave the client. Softmax advantage still drops under FedKPer, and it falls to about zero for FedLC and FedPer, but those two beat that attacker by construction. Presence can get easier while the histogram gets harder. The claim that those uploads do not leak the mix waits on a weight-level attack. The classification CNN has no BatchNorm, so that channel is not in these grids. Seismic waits.

- [x] BloodMNIST and OrganCMNIST grids (FedAvg, FedProx, FedBN, FedKPer λ)
- [x] Disjoint probe, infer-only λ=10, softmax leakage column, holdout membership at chance. Membership stays in the appendix
- [x] Debiased softmax and last-layer delta attacks, per method, on shadow clients. Worst-case x-axis is still held-out softmax advantage: Blood 0.37 → 0.15, OrganC 0.32 → 0.12 (`results/boundary_advantage.png`)
- [x] Full log π on the global model closes 87–101% of the local-accuracy gap
- [x] Staleness rerun. Softmax is scored against the global each client received. Last-upload advantage still drops: Blood 0.39 → 0.14, OrganC 0.32 → 0.12 (`results/round_advantage.csv`)
- [x] Presence vs full vs floored-proportion correction (`results/prior_modes.csv`). Presence recovers about half the full correction on Blood and about 60% on OrganC. Keeping the proportions, with absent classes only floored, recovers the full correction
- [x] Presence AUC stays high. On the saved last models, mean residual AUC is about 0.80 → 0.74 on Blood and about 0.80 → 0.77 on OrganC. True-positive rate at 1% false positives is too noisy: only 24–56 negatives per class (`results/presence_tpr.csv`)
- [x] Averaging every round raises histogram advantage for everyone and the FedKPer drop remains: Blood 0.44 → 0.21, OrganC 0.39 → 0.19. Round-pooled presence AUC goes the other way, 0.77 → 0.84 on Blood and 0.79 → 0.84 on OrganC
- [x] Frontier: Ditto is in. Its upload leaks like FedAvg and local accuracy is 0.90 Blood / 0.92 OrganC, not better than FedKPer. A final local fine-tune of the saved FedAvg global reaches 0.91 Blood / 0.95 OrganC (`results/fedavg_finetune.csv`). The server still only receives the FedAvg upload
- [x] FedPer on the same 40-round Dirichlet grid (`results/fedper_runs.csv`). The classifier stays on the client. Last-upload advantage is 0.02 Blood / 0.03 OrganC. Local accuracy is 0.93 Blood / 0.92 OrganC
- [x] FedLC, log π only at local inference (`results/logit_*_runs.csv`). Last-upload advantage falls to about 0.01 Blood / 0.03 OrganC for FedLC and about 0 for FedKPer λ=10 with the same loss. Local accuracy stays high: Blood 0.93 / 0.93, OrganC 0.94 / 0.94. Round-pooled presence AUC on that same softmax falls to roughly 0.55–0.61
- [x] Macro-F1 on classes present locally. Local still beats global: Blood FedAvg 0.73 vs 0.55, FedKPer λ=10 0.81 vs 0.66. OrganC 0.83 vs 0.75 and 0.87 vs 0.82
- [x] Worst 10% on Blood: local 0.57 → 0.71, and the global model on those same clients only goes 0.46 → 0.55. The tail gain is not already in the global model
- [x] Per-seed last-upload advantage from `results/late_advantage.csv`. Blood FedAvg is 0.403, 0.384, 0.381 (mean 0.389). FedKPer λ=10 is 0.101, 0.154, 0.168 (mean 0.141). OrganC FedAvg is 0.341, 0.310, 0.300 (mean 0.317). λ=10 is 0.130, 0.118, 0.102 (mean 0.117). The older “0.09, 0.18, 0.18” line is not this file
- [x] Rare-class presence rehearsal, 80 clients, absent vs 1–5% (`results/presence_*_presence.csv`). Rare classes are Blood 4 and 0, OrganC 2 and 3, about 240 negatives each. FedAvg → FedKPer λ=10: last-upload advantage Blood 0.18 → 0.10, OrganC 0.14 → 0.07. Presence AUC goes up, Blood 0.84 → 0.88 and OrganC 0.86 → 0.91. TPR at 1% FPR is about 0.41 → 0.73 Blood and 0.35 → 0.68 OrganC. Local accuracy does not beat the global model on this split (Blood 0.68 / 0.70 vs global 0.72, OrganC 0.74 / 0.77 vs global 0.79 / 0.81)
- [x] Classwise accuracy on the client holdout (`results/classwise_accuracy.csv`). On Blood the local model is ahead on every class. The gap is largest where the global model is weak: classes 2, 3, and 4 are about 0.89–0.91 local vs 0.24–0.27 global under FedAvg. FedKPer λ=10 lifts the global model on classes 0, 2, and 4, and class 3 stays local (0.95 vs 0.33). OrganC is the same pattern with smaller gaps
- [x] Gradient inversion, one image per client, seed 0 (`results/gradient_inversion.csv`, `results/gradient_inversion_cifar.png`). The dummy starts from noise and is matched only to the gradient. A blank image already scores about 10 dB on Blood, 16 dB on OrganC, and 12 dB on CIFAR because those images are smooth. FedAvg lands on that floor (Blood 11, OrganC 17, CIFAR 13). FedPer on CIFAR reaches 18 dB and the picture is a blurry version of the object, not a copy. Still one gradient step, not the five-epoch delta the server receives
- [x] CIFAR-10, 20 clients, 40 rounds (`results/cifar_runs.csv`, `results/boundary_cifar.png`). Last-upload advantage: FedAvg 0.49, FedKPer λ=10 0.25, FedPer 0.04, FedLC 0.05. Local holdout is about 0.87–0.89. Official test accuracy is FedAvg 0.33, FedKPer 0.52, FedPer 0.62, FedLC 0.67
- [x] Reconciled the Dirichlet table against `round_scores.jsonl` (`results/late_advantage.csv`). The means 0.39 and 0.14 match that file. The per-seed range quoted earlier does not
- [x] Late-round cut, last participation in rounds 31–40, per seed, client bootstrap (`results/late_advantage.csv`). This only drops early participations. Blood FedAvg late means are 0.41, 0.25, 0.38 against λ=10 0.08, 0.11, 0.14. Seed 1 has 11 clients and the intervals overlap. Seeds 0 and 42 do not. OrganC intervals do not overlap on any seed (FedAvg about 0.28–0.31, λ=10 about 0.08–0.10). CIFAR FedAvg stays high on the late cut: 0.38, 0.58, 0.43
- [ ] The regression of advantage on the received global’s accuracy cannot be run. `round_scores.jsonl` has no accuracy, and per-round official test scores were not saved. Across the three 20-client CIFAR FedAvg seeds, the chance seed (official test 0.12, seed 1) has the highest late advantage (0.58); the other two are official test 0.43 and 0.44 with late advantage 0.38 and 0.43. Future runs append `history.jsonl`. The job already running will not
- [x] 100-client, 100-round CIFAR official test (`results/cifar_scaled_runs.csv`). FedAvg 0.52, 0.62, 0.61 (mean 0.58). FedKPer λ=10 0.59. FedPer 0.60. FedLC 0.66. No seed is at chance. There is still no per-round curve. Advantage from this run stays unread. Do not compare these accuracies to a paper unless client count, α, participation, local epochs, rounds, and architecture all match
- [ ] Plot x becomes the max over the attack suite. Softmax, logit offset, and classifier row-norm are in `results/attack_suite.csv`, against `global_final`. Offset is not sharper than softmax. Row-norm advantage is about 0. FedPer's row norm is 0 because the saved head is the global head. FedLC present and absent row norms match on Dirichlet (0.14 vs 0.14). Shadow ridge on the full delta is not run, so FedLC and FedPer are not cleared
- [x] Per-class d′ of the softmax residual (`results/residual_dprime.csv`). It does not fall with λ. Blood Dirichlet: FedAvg 1.03, λ=0 1.07, λ=1 1.32, λ=10 1.26. FedLC is 0.32. Rehearsal: Blood 1.04 → 1.15, OrganC 1.14 → 1.06. Rows are participations
- [x] Threat model is in `findings.md`: honest-but-curious server, no secure aggregation, honest global. FedKPer numbers are `infer`, and that estimate is named as its own channel
- [x] Last-upload bias delta, softmaxed, does not beat the received global (`results/bias_advantage.csv`). Blood FedAvg about 0.05. FedKPer, FedPer, and FedLC are about 0 or negative
- [ ] Rerun the rare-class rehearsal with FedLC and FedPer on it. Same absent vs 1–5% split
- [ ] Client-cluster bootstrap, resampling whole (seed, client) groups, on presence AUC and TPR at 1% FPR. The ~240 negatives are not independent. Stratify presence by count (1–2, 3–10, >10) instead of a 1e-3 threshold on ~600 samples
- [ ] Same rehearsal at 20 clients, so a local loss is not just “80 clients means ~150 images.” The “personalization does not help when absence is real” line depends on this
- [ ] Macro-F1 on locally present classes as a second y. Local accuracy gaps of 0.01–0.02 across methods are not an ordering
- [ ] DP-FedAvg on the same axes, as the point with a formal guarantee
- [ ] Inversion: run the same one-step attack on a randomly initialized CNN. If that also sits on the blank-image floor, the attack is under-tuned. Only then try the five-epoch multi-image delta
- [ ] Seismic UNet, after the attack suite. Compare FedAvg and FedBN on the same BatchNorm UNet. FedAvg uploads running statistics computed on the client’s data, so those stats join the attack suite on the first seismic run. FedBN keeps them local. A GroupNorm UNet is the control with no running stats, so a leakage gap between it and the BatchNorm UNet is the statistics channel. Also a local segmentation head, with the row-norm and shadow attacks on the upload. Partition F3 into contiguous inline/crossline blocks, not Dirichlet
- [ ] Gradient attack on the bias stays after the attack suite above. The server sees a multi-step delta
